# ECoG RSL

Order of operations: 

1. `make_tune_tarball.m` - this wrapper script should be run locally and makes synthetic data, matching metadata, .yamls needed for the first (tune) stage of the analysis, and the correct directory tree. 
